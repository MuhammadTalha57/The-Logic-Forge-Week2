#include <bits/stdc++.h>

using namespace std;

/*
Asymptotic Analysis

Time Complexity = O(NM)
Space Complexity = O(NM)

Efficient for N, M <= O(10^3)

*/

bool maximize_freelance_profit(vector<int>& deadlines, vector<int>& profits) {
   
}

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);

    // string text = "ab", pattern = "*a";
    // cout << solve(text, pattern);


    return 0;
}